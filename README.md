# Register_form
Responsive registration form with real-time validation using HTML, CSS, and JavaScript. Validates name, email, and password inputs, displays instant error messages, and enables submission only when all fields are valid.
